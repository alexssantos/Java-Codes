# Curso Udemy java 

# Sesssão 1 --

- Comandos:
    - Conversão 
        (int)
        integer.ParseInt()

    - Print
        System.out.println
        System.out.print
        System.out.printf

    - Scanner = new Scanner(System.in);
        sc.next()
        sc.nextLine()
        sc.nextInt()
        sc.close()

    - String 
        texto.substring()

    - String Format
        Formatar > toLowerCase(), toUpperCase(), trim()
        Rocortar > substring(inicio), substring(inicio,fim)
        Substituir > Replace(char,char), Replace(str, str)
        Buscar > indexOf, LastIndexOf
        Subdividir > srt.Split(" ")

    - Patterns Format
        Locale.serDefault(Locale.US)

    